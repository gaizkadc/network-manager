###
# Application network-manager
###

kind: Deployment
apiVersion: apps/v1
metadata:
  labels:
    cluster: management
    component: network-manager
  name: network-manager
  namespace: nalej
spec:
  replicas: 1
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      cluster: management
      component: network-manager
  template:
    metadata:
      labels:
        cluster: management
        component: network-manager
    spec:
      containers:
      - name: network-manager
        image: nalejregistry.azurecr.io/nalej/network-manager:VERSION
        imagePullPolicy: IfNotPresent
        args:
          - "run"
          - "--dnsurl=http://dns-server-ui.nalej:80"
          - "--sm=system-model.nalej:8800"
          - "--zturl=http://localhost:9993"
          - "--debug"
        securityContext:
          runAsUser: 0
        volumeMounts:
        - name: dev-net-tun
          readOnly: true
          mountPath: /dev/net/tun
        - name: data-network-manager
          mountPath: /mnt/data
      imagePullSecrets:
      - name: nalej-registry
      volumes:
      - name: dev-net-tun
        hostPath:
          path: /dev/net/tun
      - name: data-network-manager
        hostPath:
          path: /mnt/data
